#Ευκλείδεια απόσταση
import math    # εισάγουμε την βιβλιοθήκη
p1=float(input("Δώσε το to p1:"))
p2=float(input("Δώσε το to p2:"))
q1=float(input("Δώσε το to q1:"))
q2=float(input("Δώσε το to q1:"))
d=math.sqrt((q1-p1)**2+(p2-q2)**2) # η sqrt υπολογίζει την τετραγωνική ρίζα. Στις παρενθέσεις εφαρμόζεται η προτεραιότητα των πράξεων
print("Η απόσταση είναι:",d)
